import tensorflow as tf
import os
import numpy as np
import cv2

def load_MNIST():
    """Loading MNIST dataset

    Rets:
        trX: (50000, 32, 32, 3)     training images tensor
        trY: (50000, 10)            training images labels, all one-hot vectors
        teX: (10000, 32, 32, 3)     testing  images tensor
        teY: (10000, 10)            testing  images labels, all one-hot vectors
    """
    pass

def load_MNIST_M():
    """loading MNIST-M dataset

    rets:
        trx: (50000, 32, 32, 3)     training images tensor
        try: (50000, 10)            training images labels, all one-hot vectors
        tex: (10000, 32, 32, 3)     testing  images tensor
        tey: (10000, 10)            testing  images labels, all one-hot vectors
    """
    pass

def load_USPS():
    """loading USPS dataset

    rets:
        trx: (50000, 32, 32, 3)     training images tensor
        try: (50000, 10)            training images labels, all one-hot vectors
        tex: (10000, 32, 32, 3)     testing  images tensor
        tey: (10000, 10)            testing  images labels, all one-hot vectors
    """
    pass

def load_SVNH():
    """loading SVHN dataset

    rets:
        trx: (50000, 32, 32, 3)     training images tensor
        try: (50000, 10)            training images labels, all one-hot vectors
        tex: (10000, 32, 32, 3)     testing  images tensor
        tey: (10000, 10)            testing  images labels, all one-hot vectors
    """
    pass

